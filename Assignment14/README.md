# Python Assignment 14

This assignment focuses on lambda functions and functional programming fundamentals in Python, covering basic mathematical and logical operations using anonymous functions.

---

## 📚 Topics Covered

- Lambda functions
- Anonymous functions
- Mathematical operations
- Logical operations
- Comparison functions

---

## 🎯 Questions

### 1. Square of a Number
Write a lambda function which accepts one number and returns square of that number.

**Example:**
```python
square = lambda x: x**2
Result: square(5) → 25
```

---

### 2. Cube of a Number
Write a lambda function which accepts one number and returns cube of that number.

**Example:**
```python
cube = lambda x: x**3
Result: cube(3) → 27
```

---

### 3. Maximum of Two Numbers
Write a lambda function which accepts two numbers and returns maximum number.

**Example:**
```python
max_num = lambda x, y: x if x > y else y
Result: max_num(10, 20) → 20
```

---

### 4. Minimum of Two Numbers
Write a lambda function which accepts two numbers and returns minimum number.

**Example:**
```python
min_num = lambda x, y: x if x < y else y
Result: min_num(10, 20) → 10
```

---

### 5. Check Even Number
Write a lambda function which accepts one number and returns True if number is even otherwise False.

**Example:**
```python
is_even = lambda x: x % 2 == 0
Result: is_even(4) → True
```

---

### 6. Check Odd Number
Write a lambda function which accepts one number and returns True if number is odd otherwise False.

**Example:**
```python
is_odd = lambda x: x % 2 != 0
Result: is_odd(5) → True
```

---

### 7. Check Divisibility by 5
Write a lambda function which accepts one number and returns True if divisible by 5.

**Example:**
```python
div_by_5 = lambda x: x % 5 == 0
Result: div_by_5(15) → True
```

---

### 8. Addition of Two Numbers
Write a lambda function which accepts two numbers and returns addition.

**Example:**
```python
add = lambda x, y: x + y
Result: add(10, 20) → 30
```

---

### 9. Multiplication of Two Numbers
Write a lambda function which accepts two numbers and returns multiplication.

**Example:**
```python
multiply = lambda x, y: x * y
Result: multiply(5, 6) → 30
```

---

### 10. Largest of Three Numbers
Write a lambda function which accepts three numbers and returns largest number.

**Example:**
```python
largest = lambda x, y, z: max(x, y, z)
Result: largest(10, 25, 15) → 25
```